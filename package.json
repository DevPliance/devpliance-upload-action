{
  "name": "devplace-upload-action",
  "version": "0.1.0",
  "description": "GitHub Action to upload data to DevPlace",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "build": "ncc build src/main.ts -o dist --source-map --license licenses.txt",
    "dev": "ts-node src/main.ts"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vercel/ncc": "^0.38.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
